# 方式一（推荐本地开发）：留空，请求走相对路径 /api/v1，由 Vite 代理到后端。
# 代理目标默认 http://127.0.0.1:8000；若后端端口不同，请设置下一行。
# VITE_API_BASE_URL=

# 仅当后端不在 8000 且未设置 VITE_API_BASE_URL 时需要（与直连二选一即可）
# VITE_API_PROXY_TARGET=http://127.0.0.1:8009

# 方式二：直连后端（需后端 CORS_ORIGINS 包含本前端源，如 http://127.0.0.1:3679）
# VITE_API_BASE_URL=http://127.0.0.1:8009
